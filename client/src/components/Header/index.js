import React from 'react';
import { Container } from './style';

const Header = (props) => {

    return (
        <Container>
            <h2>Home</h2>
        </Container>
    );
}

export default Header;