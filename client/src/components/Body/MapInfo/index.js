import React from 'react';
import { Container, MapDiv } from './style';

const MapInfo = props => {
    return (
        <Container>
            <span>Map info</span>
            <MapDiv />
        </Container>
    );
}

export default MapInfo;